<header>
	<h1>Enjoymsg</h1>
	<%if session[:user_id]%> 
	<%= image_tag current_user.avatar.url(:s32) %>, 
	<%=link_to current_user.fullname,current_user %>,
	<%=link_to 'logout',logout_path,:method=>:delete %>
	<%else %>
	<a href ="/auth">login</a> | <a href ="/register">register</a>
	<%
      end
	%>
</header>